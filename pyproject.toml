[project]
name = "ha-vk"
version = "0.2.0"
description = "Home Assistant custom integration for VK notifications"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = [
  "tests"
]

[tool.ruff]
target-version = "py312"
line-length = 100
